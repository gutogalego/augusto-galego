# PTPTPTPTPTP BRBRBRBRBR

1. **Statelessness**: Each request from a client to a server must contain all the information needed to understand and process the request.
2. **Connectivity**: It uses a connectionless communication model, meaning that, by default, a connection is established and closed for each request.
3. **Extensibility**: HTTP headers let you pass optional parameters with your request or response. Plus, they can be custom-defined.

## HTTP Methods

There are several methods HTTP uses to interact with resources:



Each of these methods (or "verbs") defines a different action to be performed on the specified resource.

Understanding HTTP is crucial for web development, as it underpins how data is exchanged on the internet. Dive deeper into each method and header to truly master web communications.

